 <main role="main">
    {{#if loggedIn}}
      <section class="jumbotron text-center">
        <div class="container">
          <!--<h1 class="jumbotron-heading">Album example</h1> -->
          
          <img src="./images/thisLogo.png" style="width: 200px; height: 200px;" alt="Logo Image">
         
          <p class="lead text-muted">Welcome to the MovieMentor, a great place to discover what's new in the world of movies! </p>
        </div>
      </section>


      <div class="album py-5 bg-light">
        <div class="container">

          <div class="row">
            {{#each movies as |movie|}}
            {{> movie-card}}
            {{/each}} 
            <!--
            {{#each shows as |show|}}
            <div class="col-md-4">
              <div class="card mb-4 box-shadow">
                <img class="card-img-top" data-src="holder.js/100px225?theme=thumb&bg=55595c&fg=eceeef&text=Thumbnail" alt="Card image cap">
                <div class="card-body">
                  <p class="card-text">Movie 1</p>
                  <div class="d-flex justify-content-between align-items-center">
                    <div class="btn-group">
                      <button type="button" class="btn btn-sm btn-outline-secondary">View</button>
                      <button type="button" class="btn btn-sm btn-outline-secondary">Add to favorites +</button>
                    </div>
                    <small class="text-muted">runtime</small>
                  </div>
                </div>
              </div>
            </div>
            {{/each}}
            -->
          </div>
        </div>
      </div>
      {{else}}
      <a href="/login">You must login first to view this page</a>
      {{/if}}
     </main>